#ifndef _TEST_VECTOR_H_
#define _TEST_VECTOR_H_

#include "test_utils.h"

/*!
 * Runs all the vector tests.
 * \return SUCCESS if all tests pass, FAILURE if one or more tests fail.
 */
uint test_vector(void);

#endif // _TEST_VECTOR_H_
