#ifndef _TEST_GRID_H_
#define _TEST_GRID_H_

#include "test_utils.h"

/*!
 * Runs all the grid tests.
 * \return SUCCESS if all tests pass, FAILURE if one or more tests fail.
 */
uint test_grid(void);

#endif // _TEST_GRID_H_
