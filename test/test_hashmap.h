#ifndef _TEST_HASHMAP_H_
#define _TEST_HASHMAP_H_

#include "test_utils.h"

/*!
 * Runs all the hashmap tests.
 * \return SUCCESS if all tests pass, FAILURE if one or more tests fail.
 */
uint test_hashmap(void);

#endif // _TEST_HASHMAP_H_
